version: '3'
services:
  cloud-musiclinker:
    image: lee1080/cloudmusic-linker:latest
    container_name: cloudmusic-linker
    restart: always
    ports:
      - "3010:3000"
    environment:
      - TZ=Asia/Shanghai
